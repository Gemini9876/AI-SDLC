import datetime\nimport time\n\nfrom behave import *\n\n# --- Mock Data and Utilities ---\n# This dictionary holds the initial state for users and will be reset before each scenario.\n_initial_mock_users_db_state = {\n    'freeuser@example.com': {'password': 'FreePass123', 'subscription': 'Free', 'failed_attempts': 0, 'locked_until': None},\n    'paiduser@example.com': {'password': 'PaidPass123', 'subscription': 'Basic-level Paid', 'failed_attempts': 0, 'locked_until': None},\n    'testuser@example.com': {'password': 'CorrectPassword', 'subscription': 'Free', 'failed_attempts': 0, 'locked_until': None}\n}\n# This is the mutable version used during test execution.\nmock_users_db = _initial_mock_users_db_state.copy()\n\nmock_articles_db = {\n    'Free Article: Getting Started': {'type': 'Free', 'url': '/articles/getting-started-free-id', 'alt_text': 'Thumbnail for \'Getting Started\' article, depicting a person reading a book.'},\n    'Paid Article: Advanced Techniques': {'type': 'Paid', 'url': '/articles/advanced-techniques-paid-id', 'alt_text': 'Thumbnail for \'Advanced Techniques\' article, depicting complex data visualizations.'}\n}\n\n# --- Environment Hooks (simulating features/environment.py) ---\ndef before_scenario(context, scenario):\n    # Reset context variables for each scenario to ensure test independence.\n    context.current_page = None\n    context.email_field = ''\n    context.password_field = ''\n    context.logged_in_user = None\n    context.error_message = None\n    context.visible_articles = []\n    context.simulated_navigation_history = []\n    context.lockout_threshold = 5 # Default threshold\n    context.lockout_duration_minutes = 15 # Default duration\n    context.paid_article_viewable = False\n    context.performance_load_time_ms = 0\n    context.simulated_url = None\n    context.focusable_elements_order = []\n    context.current_focus_index = -1\n    context.current_focused_element = None\n    context.tab_order_traversed = []\n    context.screen_reader_active = False\n    context.announced_alt_text = None\n    context.contrast_results = {}\n    context.responsive_view_tested = False\n    context.layout_correct = False\n\n    # Reset mock user data for each scenario to ensure independence.\n    # We copy from the initial state to ensure a clean slate.\n    global mock_users_db\n    mock_users_db = _initial_mock_users_db_state.copy()\n\n# --- Step Definitions ---\n\n@given('User \'{email}\' exists with \'{subscription}\' subscription and the application\'s \'Login\' page is accessible on a mobile device.')\ndef step_user_exists_and_login_page_accessible(context, email, subscription):\n    # Ensure user exists in our mock DB for this test run.\n    # The global mock_users_db is reset in before_scenario, so we add/update as needed.\n    if email not in mock_users_db:\n        mock_users_db[email] = {'password': 'SomePass', 'subscription': subscription, 'failed_attempts': 0, 'locked_until': None}\n    else:\n        # Reset state for user if already exists from previous scenario (redundant with before_scenario but safe).\n        mock_users_db[email]['subscription'] = subscription\n        mock_users_db[email]['failed_attempts'] = 0\n        mock_users_db[email]['locked_until'] = None\n    \n    context.current_page = 'Login'\n\n@given('Application\'s \'Login\' page is accessible on a mobile device.')\ndef step_login_page_accessible(context):\n    context.current_page = 'Login'\n\n@given('Application is deployed and accessible, and performance testing tools are set up to simulate concurrent mobile users and measure load times.')\ndef step_performance_setup(context):\n    # No direct action, setup is assumed. Context variables initialized in before_scenario.\n    pass\n\n@given('A \'Free\' subscriber account is logged in on a mobile device, and Paid article \'Advanced Techniques\' exists with a direct URL \'{url}\'.')\ndef step_free_user_logged_in_paid_article_exists(context, url):\n    # Simulate a free user being logged in.\n    context.logged_in_user = 'freeuser@example.com'\n    context.current_page = 'Home' # Assume they are on Home page after login.\n    context.simulated_url = url\n    # Verify the mock article exists for consistency.\n    assert 'Paid Article: Advanced Techniques' in mock_articles_db\n    assert mock_articles_db['Paid Article: Advanced Techniques']['url'] == url\n\n@given('A valid user account \'{email}\' exists, and account lockout mechanism is enabled with a threshold of {threshold:d} failed attempts within a defined period.')\ndef step_account_lockout_enabled(context, email, threshold):\n    # Ensure the user exists and reset their failed attempts/lockout status.\n    if email in mock_users_db:\n        mock_users_db[email]['failed_attempts'] = 0\n        mock_users_db[email]['locked_until'] = None\n    else:\n        # Create user if they don't exist in mock DB.\n        mock_users_db[email] = {'password': 'CorrectPassword', 'subscription': 'Free', 'failed_attempts': 0, 'locked_until': None}\n    context.lockout_threshold = threshold\n    context.email_field = '' # Clear fields from previous potential steps.\n    context.password_field = ''\n    context.lockout_duration_minutes = 15 # Set standard lockout duration for clarity.\n\n@given('User is logged in as a \'{subscription_type}\' subscriber on a mobile device, and the \'Home\' page displaying article titles/thumbnails is accessible.')\ndef step_user_logged_in_home_page_accessible(context, subscription_type):\n    # Simulate login based on subscription type.\n    if subscription_type == 'Paid':\n        context.logged_in_user = 'paiduser@example.com'\n        context.visible_articles = ['Free Article: Getting Started', 'Paid Article: Advanced Techniques']\n    elif subscription_type == 'Free':\n        context.logged_in_user = 'freeuser@example.com'\n        context.visible_articles = ['Free Article: Getting Started']\n    context.current_page = 'Home'\n\n@given('Application\'s \'Login\' page is accessible, and a physical keyboard or an accessibility tool simulating keyboard navigation is available for the mobile device/emulator.')\ndef step_login_page_keyboard_available(context):\n    context.current_page = 'Login'\n    context.focusable_elements_order = ['Email field', 'Password field', 'Login button']\n    context.current_focus_index = -1 # Indicates no focus initially, or before first tab press.\n\n@given('User is logged in and on the \'Home\' page displaying article thumbnails on a mobile device, and a mobile screen reader is enabled.')\ndef step_home_page_screen_reader_enabled(context):\n    context.logged_in_user = 'paiduser@example.com' # Any logged-in user can be on Home page.\n    context.current_page = 'Home'\n    context.screen_reader_active = True\n\n@given('Application is accessible on mobile devices/emulators with various screen dimensions.')\ndef step_app_accessible_various_screen_sizes(context):\n    # This step assumes the environment is set up. No specific state to set in context.\n    pass\n\n@when('I open the application on a mobile device and navigate to the \'Login\' page')\ndef step_open_app_navigate_to_login(context):\n    context.current_page = 'Login'\n    context.simulated_navigation_history.append('Login')\n\n@when('I enter \'{value}\' into the \'Email\' field')\ndef step_enter_email(context, value):\n    context.email_field = value\n\n@when('I enter \'{value}\' into the \'Password\' field')\ndef step_enter_password(context, value):\n    context.password_field = value\n\n@when('I tap the \'Login\' button')\ndef step_tap_login_button(context):\n    email = context.email_field\n    password = context.password_field\n\n    # Simulate frontend validation first\n    if not email:\n        context.error_message = 'Email is required.'\n        return\n    if not password:\n        context.error_message = 'Password is required.'\n        return\n    # Basic email format validation (missing domain part)\n    if '@' not in email or '.' not in email.split('@')[-1] or email.split('@')[-1].startswith('.'):\n        context.error_message = 'Please enter a valid email address.'\n        return\n\n    user_data = mock_users_db.get(email)\n\n    if user_data:\n        # Check for account lockout before attempting login.\n        if user_data['locked_until'] and user_data['locked_until'] > datetime.datetime.now():\n            context.error_message = f'Your account has been locked due to too many failed login attempts. Please try again in {context.lockout_duration_minutes} minutes or reset your password.'\n            context.current_page = 'Login'\n            return\n\n        if user_data['password'] == password:\n            # Successful login.\n            context.logged_in_user = email\n            context.current_page = 'Home'\n            context.simulated_navigation_history.append('Home')\n            user_data['failed_attempts'] = 0 # Reset failed attempts on successful login.\n            context.error_message = None # Clear any previous error.\n            \n            # Set visible articles based on user's subscription.\n            context.visible_articles = []\n            if user_data['subscription'] == 'Free':\n                context.visible_articles.append('Free Article: Getting Started')\n            elif user_data['subscription'] == 'Basic-level Paid':\n                context.visible_articles.append('Free Article: Getting Started')\n                context.visible_articles.append('Paid Article: Advanced Techniques')\n        else:\n            # Incorrect password.\n            context.current_page = 'Login'\n            user_data['failed_attempts'] += 1\n            # Check for lockout after incrementing failed attempts.\n            if user_data['failed_attempts'] >= context.lockout_threshold:\n                user_data['locked_until'] = datetime.datetime.now() + datetime.timedelta(minutes=context.lockout_duration_minutes)\n                context.error_message = f'Your account has been locked due to too many failed login attempts. Please try again in {context.lockout_duration_minutes} minutes or reset your password.'\n            else:\n                context.error_message = 'Invalid email or password. Please try again.'\n    else:\n        # User not found.\n        context.current_page = 'Login'\n        context.error_message = 'Invalid email or password. Please try again.'\n\n@when('I simulate {num_users:d} concurrent mobile users ({num_paid:d} paid, {num_free:d} free) logging in simultaneously and navigating to the \'Home\' page')\ndef step_simulate_concurrent_users(context, num_users, num_paid, num_free):\n    # Simulate load time. A higher number of users results in a longer load time.\n    # This is a fixed simulation for demonstration.\n    simulated_load_factor = (num_users / 1000.0) # Assume 1000 users is a baseline for calculation.\n    context.performance_load_time_ms = 2500 * simulated_load_factor # Baseline 2.5s for 1000 users.\n\n@when('I attempt to navigate directly to the URL for a \'Paid Article: Advanced Techniques\'')\ndef step_attempt_direct_navigation_to_paid_article(context):\n    if context.logged_in_user:\n        user_data = mock_users_db.get(context.logged_in_user)\n        if user_data and user_data['subscription'] == 'Free':\n            # Free user trying to access paid content.\n            context.current_page = 'Subscription Upgrade'\n            context.simulated_navigation_history.append('Subscription Upgrade')\n            context.paid_article_viewable = False\n        else:\n            # Paid user or unknown user (assume access if not explicitly free and restricted).\n            context.current_page = 'Paid Article: Advanced Techniques'\n            context.simulated_navigation_history.append('Paid Article: Advanced Techniques')\n            context.paid_article_viewable = True\n    else:\n        # Not logged in, typically redirects to login or subscription page.\n        context.current_page = 'Login'\n        context.simulated_navigation_history.append('Login')\n        context.paid_article_viewable = False\n\n@when('for {attempts:d} consecutive times, I enter \'{email}\' into the \'Email\' field, then enter an incorrect password \'{password}\' into the \'Password\' field, and tap the \'Login\' button')\ndef step_multiple_failed_login_attempts(context, attempts, email, password):\n    context.email_field = email # Set email for all attempts.\n\n    for _ in range(attempts):\n        context.password_field = password # Set incorrect password for each attempt.\n        # Simulate the login tap logic for each attempt.\n        step_tap_login_button(context)\n        # In a real test, you'd observe the UI after each tap.\n        # Here, we're simulating the internal state changes.\n        # If an error occurs that is NOT lockout, clear it for the next attempt.\n        # Lockout message will persist.\n        if context.error_message and not ("locked" in context.error_message):\n            context.error_message = None\n        context.current_page = 'Login' # User remains on login page.\n\n@when('I leave the \'Email\' field blank')\ndef step_leave_email_blank(context):\n    context.email_field = ''\n\n@when('On the \'Home\' page, I tap on the \'{article_title}\' title or thumbnail')\ndef step_tap_article_title_thumbnail(context, article_title):\n    assert context.current_page == 'Home', f"Expected to be on 'Home' page to tap, but on '{context.current_page}'"\n    assert article_title in context.visible_articles, f"Article '{article_title}' is not visible to tap."\n    context.current_page = f'Full Content Page: {article_title}'\n    context.simulated_navigation_history.append(f'Full Content Page: {article_title}')\n\n@when('I ensure focus is initially on an interactive element')\ndef step_ensure_initial_focus(context):\n    # Simulate initial focus on the first interactive element as per common UI behavior.\n    context.current_focus_index = 0\n    context.current_focused_element = context.focusable_elements_order[0]\n    context.tab_order_traversed.append(context.current_focused_element) # Add initial focus to sequence.\n\n@when('I press the \'Tab\' key repeatedly')\ndef step_press_tab_repeatedly(context):\n    # Simulate tabbing through all focusable elements once in sequence.\n    if context.current_focus_index == -1: # If no initial focus set by previous step.\n        context.current_focus_index = 0\n        context.tab_order_traversed.append(context.focusable_elements_order[context.current_focus_index]) # Add initial focus.\n    \n    # Simulate tabbing from current focus to the end, then wrap around.\n    # We'll collect the order for verification in the 'Then' step.\n    for _ in range(len(context.focusable_elements_order) - 1): # Tab N-1 times if N elements, as initial is already set.\n        context.current_focus_index = (context.current_focus_index + 1) % len(context.focusable_elements_order)\n        context.current_focused_element = context.focusable_elements_order[context.current_focus_index]\n        context.tab_order_traversed.append(context.current_focused_element)\n\n@when('With the mobile screen reader active, I navigate and focus on the image representing \'{article_title}\'')\ndef step_screen_reader_focus_image(context, article_title):\n    assert context.screen_reader_active, "Screen reader is not active."\n    # Simulate focusing on the image and capturing its alt text from mock data.\n    if article_title in mock_articles_db:\n        context.announced_alt_text = mock_articles_db[article_title]['alt_text']\n    else:\n        context.announced_alt_text = None # Article not found or no alt text.\n\n@when('Using a color contrast analyzer tool, I inspect the text and background colors of form labels, input fields, and the \'Login\' button')\ndef step_inspect_color_contrast(context):\n    # Simulate the outcome of a color contrast inspection.\n    # For this simulated environment, we assume compliance by default for the 'When' action.\n    context.contrast_results = {\n        'labels': {'ratio': 4.6, 'meets_wcag': True},\n        'input_fields': {'ratio': 4.5, 'meets_wcag': True}, # Assuming text inside input fields is normal.\n        'login_button': {'ratio': 5.0, 'meets_wcag': True} # Assuming button is a graphic element / large text.\n    }\n\n@when('I log in to the application on a mobile device (or emulator/browser with device mode enabled) and navigate to the \'Home\' page')\ndef step_login_and_navigate_home_for_responsive(context):\n    context.logged_in_user = 'paiduser@example.com' # Simulate successful login.\n    context.current_page = 'Home'\n\n@when('I view the \'Home\' page on various mobile screen sizes and orientations (e.g., small smartphone portrait, large smartphone landscape, tablet portrait, tablet landscape)')\ndef step_view_home_page_on_various_screens(context):\n    # Simulate this action being performed and recording its outcome.\n    context.responsive_view_tested = True\n    context.layout_correct = True # Assume success for the 'When' part, verification is in 'Then'.\n\n@when('I leave the \'Password\' field blank')\ndef step_leave_password_blank(context):\n    context.password_field = ''\n\n\n@then('The user is redirected to the \'Home\' page')\ndef step_user_redirected_to_home(context):\n    assert context.current_page == 'Home', f"Expected to be on 'Home' page, but currently on '{context.current_page}'."\n    assert 'Home' == context.simulated_navigation_history[-1], f"Last navigation in history was not to 'Home', but to '{context.simulated_navigation_history[-1]}'."\n\n@then('\'{article_title}\' is displayed')\ndef step_article_is_displayed(context, article_title):\n    assert article_title in context.visible_articles, f"Expected '{article_title}' to be displayed, but it is not."\n\n@then('\'{article_title}\' is NOT displayed.')\ndef step_article_is_not_displayed(context, article_title):\n    assert article_title not in context.visible_articles, f"Expected '{article_title}' NOT to be displayed, but it is."\n\n@then('The user remains on the \'Login\' page')\ndef step_user_remains_on_login(context):\n    assert context.current_page == 'Login', f"Expected to remain on 'Login' page, but currently on '{context.current_page}'."\n\n@then('An error message \'{message}\' is displayed.')\ndef step_error_message_is_displayed(context, message):\n    assert context.error_message == message, f"Expected error message '{message}', but got '{context.error_message}'."\n\n@then('The \'Home\' page fully loads for all simulated mobile users within {time_limit:g} seconds.')\ndef step_home_page_loads_within_time(context, time_limit):\n    # Convert seconds to milliseconds for comparison as our simulation uses milliseconds.\n    time_limit_ms = time_limit * 1000\n    assert context.performance_load_time_ms <= time_limit_ms, \n        f"Simulated Home page load time was {context.performance_load_time_ms}ms, which exceeds the {time_limit_ms}ms limit."\n\n@then('The user is redirected to the \'Subscription Upgrade\' page')\ndef step_redirected_to_subscription_upgrade(context):\n    assert context.current_page == 'Subscription Upgrade', f"Expected to be on 'Subscription Upgrade' page, but currently on '{context.current_page}'."\n    assert 'Subscription Upgrade' == context.simulated_navigation_history[-1], f"Last navigation in history was not to 'Subscription Upgrade', but to '{context.simulated_navigation_history[-1]}'."\n\n@then('The content of the paid article is NOT viewable.')\ndef step_paid_article_not_viewable(context):\n    assert not context.paid_article_viewable, "Paid article content was viewable, but it should not have been for a free user."\n\n@then('On the {attempt_number_str} attempt, the account \'{email}\' is temporarily locked by the system')\ndef step_account_locked_on_attempt(context, attempt_number_str, email):\n    # This assertion verifies the state after the series of attempts in the 'When' step.\n    user_data = mock_users_db.get(email)\n    assert user_data is not None, f"User '{email}' not found in mock_users_db."\n    # Verify that the account is indeed locked and the lock has not yet expired.\n    assert user_data['locked_until'] is not None and user_data['locked_until'] > datetime.datetime.now(), \n        f"Account '{email}' is not locked or lock has expired. Locked until: {user_data['locked_until']} (Current time: {datetime.datetime.now()})"\n    # The 'attempt_number_str' is just for description, the 'When' step ensures the lockout logic triggers at the correct attempt.\n\n@then('A message \'{message}\' is displayed on the \'Login\' page.')\ndef step_message_displayed_on_login(context, message):\n    assert context.current_page == 'Login', f"Expected message on 'Login' page, but currently on '{context.current_page}'."\n    assert context.error_message == message, f"Expected message '{message}', but got '{context.error_message}'."\n\n@then('An inline error message \'{message}\' is displayed next to the \'Email\' field.')\ndef step_inline_error_message_email(context, message):\n    assert context.error_message == message, f"Expected inline error message for Email: '{message}', but got '{context.error_message}'."\n    # In a real UI test, you would also verify its position on the screen.\n\n@then('The user is navigated to the full content page for \'{article_title}\'.')\ndef step_navigated_to_full_content(context, article_title):\n    expected_page = f'Full Content Page: {article_title}'\n    assert context.current_page == expected_page, f"Expected to be on '{expected_page}', but currently on '{context.current_page}'."\n    assert expected_page == context.simulated_navigation_history[-1], f"Last navigation in history was not to '{expected_page}', but to '{context.simulated_navigation_history[-1]}'."\n\n@then('The focus should move sequentially from the \'Email\' input field to the \'Password\' input field to the \'Login\' button, without skipping elements or jumping unexpectedly.')\ndef step_focus_moves_sequentially(context):\n    expected_order = ['Email field', 'Password field', 'Login button']\n    assert context.tab_order_traversed == expected_order, \n        f"Expected tab order: {expected_order}, but got: {context.tab_order_traversed}."\n    assert len(set(context.tab_order_traversed)) == len(expected_order), "Focus skipped or repeated elements."\n\n@then('The screen reader announces the \'alt\' attribute value \'{alt_text}\' for the {article_type} Article image')\ndef step_screen_reader_announces_alt_text(context, alt_text, article_type):\n    assert context.announced_alt_text == alt_text, f"Screen reader announced '{context.announced_alt_text}', but expected '{alt_text}'."\n\n@then('All text and interactive elements on the \'Login\' page meet WCAG 2.1 AA contrast ratio requirements (at least {normal_text_ratio:g}:1 for normal text, {large_text_ratio:g}:1 for large text and graphic elements).')\ndef step_check_wcag_contrast_ratio(context, normal_text_ratio, large_text_ratio):\n    # Verify that the simulated contrast results meet the WCAG requirements.\n    assert context.contrast_results['labels']['meets_wcag'] and context.contrast_results['labels']['ratio'] >= normal_text_ratio, \n        f"Labels do not meet WCAG contrast ratio ({context.contrast_results['labels']['ratio']}:1 vs required {normal_text_ratio}:1)."\n    assert context.contrast_results['input_fields']['meets_wcag'] and context.contrast_results['input_fields']['ratio'] >= normal_text_ratio, \n        f"Input fields do not meet WCAG contrast ratio ({context.contrast_results['input_fields']['ratio']}:1 vs required {normal_text_ratio}:1)."\n    assert context.contrast_results['login_button']['meets_wcag'] and context.contrast_results['login_button']['ratio'] >= large_text_ratio, \n        f"Login button does not meet WCAG contrast ratio ({context.contrast_results['login_button']['ratio']}:1 vs required {large_text_ratio}:1)."\n\n@then('The \'Home\' page layout adjusts correctly to different screen sizes and orientations; content is readable, images scale appropriately, and interactive elements are accessible without horizontal scrolling or overlapping.')\ndef step_home_page_responsive_layout(context):\n    assert context.responsive_view_tested, "Responsive view was not tested in the 'When' step."\n    assert context.layout_correct, "Home page layout did not adjust correctly across different screen sizes and orientations."\n\n@then('An inline error message \'{message}\' or similar (e.g., \'Invalid domain format\') is displayed next to the \'Email\' field.')\ndef step_inline_error_message_email_or_similar(context, message):\n    # This step allows for slight variations in the error message for invalid email format.\n    expected_messages = [message, 'Invalid domain format', 'Please enter a valid email address.']\n    assert context.error_message in expected_messages, \n        f"Expected inline error message for Email to be one of {expected_messages}, but got '{context.error_message}'."\n\n@then('An inline error message \'{message}\' is displayed next to the \'Password\' field.')\ndef step_inline_error_message_password(context, message):\n    assert context.error_message == message, f"Expected inline error message for Password: '{message}', but got '{context.error_message}'."